# 生产环境必须替换为随机生成且至少 32 位的密钥
APP_JWT_SECRET=replace-with-a-random-secret-at-least-32-characters
APP_JWT_EXPIRE_SECONDS=3600
APP_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173

# 仅用于本地演示：首次启动时为尚无凭证的演示用户设置统一密码
APP_DEMO_PASSWORD=
