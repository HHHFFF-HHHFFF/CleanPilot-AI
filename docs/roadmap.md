# 交付路线图

## 已完成

- 知识库操作：文档状态、上传、注入扫描、重试和索引移除。
- 业务数据基础：从带版本号的 CSV 文件导入的 SQLite 用户、设备和月度使用记录。
- RAG 质量保障：15 条标注用例、Recall@K 和 MRR 评测，以及 JSON 报告输出。
- 客服工作台：演示用户选择、浏览器授权定位、实时天气和可审阅的工具调用摘要。
- 多 Agent 编排：调度 Agent、知识问答 Agent、故障诊断 Agent、用户运营 Agent，以及结构化路由、最小工具权限和用户数据强制绑定。
- API 身份闭环：FastAPI 登录、PBKDF2 密码哈希、短期 JWT、当前用户/设备接口，以及按令牌用户执行的 NDJSON 流式多 Agent 对话。
- React 用户端：登录、会话身份恢复、设备概览、自动定位天气、流式 Agent 处理摘要、Markdown 回答和响应式布局。
- 账户会话历史：SQLite 会话与消息表、按用户隔离的增删查 API、流式结果自动落库，以及桌面左栏和移动端历史抽屉。
- 管理员知识库闭环：SQLite 角色、JWT 声明与 FastAPI RBAC 校验；React 管理员入口支持状态查看、同步、安全上传、重试入库和索引移除。
- 故障诊断设备上下文：诊断 Skill 可查询当前用户绑定的设备型号与保修信息，查询身份由运行时强制绑定并受 Skill 工具白名单保护。
- 业务 Skill 编排：故障分级诊断与用户月度运营报告 SOP、注册表白名单映射、动态 Prompt 注入和 Skill 工具权限拦截。
- 分层记忆闭环：最近消息工作记忆、较早会话滚动摘要、低敏感用户画像、故障事件记忆、按 Agent/Skill 分类召回、版本更新、180 天过期、每 6 小时容量压缩和逻辑删除策略。
- 用户记忆控制台：React 页面支持按类型查看当前账户记忆，并允许用户修改错误内容或主动遗忘。

## 下一步

- 添加数据导入任务、审计日志和内容所有者审核工作流。
- 增加 React 设备详情、历史报告和账户设置页面；Streamlit 仅保留为本地知识库调试入口。
- 增加刷新令牌、登录限流、审计日志、账户管理，并集成真实工单/CRM 系统。
- 在现有检索评测基础上，添加工具调用成功率、答案质量、反馈收集和生产环境监控功能。
- 增加用户画像授权开关、记忆变更审计，并继续完善敏感信息分级策略。
- 增加人工转接与客服反馈闭环。
- 为故障诊断 Agent 增加多模态图片识别，并继续沉淀保修查询、工单创建等可复用 Skill。
